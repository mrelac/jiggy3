name: jiggy3
description: Jiggy version 3

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter_bloc: ^6.1.1
  sqflite: ^1.3.0
  path_provider: ^1.6.5
  connectivity: ^2.0.1
  intl: ^0.16.1
  image_cropper: ^1.3.1
  image_picker: ^0.6.7+21
  exif: ^1.0.3
  fab_circular_menu: ^1.0.0
  image: ^2.1.14
  flutter_colorpicker: ^0.3.4
  provider: ^4.3.2+2
  json_serializable: ^3.5.1
  reorderables: ^0.3.2

  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  test: any

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/img/dow-farm-1.jpg
    - assets/img/lim-1.jpg
    - assets/img/lim-2.jpg
    - assets/img/lim-3.jpg
    - assets/img/Flowers.jpg
    - assets/img/Snowy.jpg
    - assets/img/Shore.jpg
    - assets/img/Cove.jpg
    - assets/img/Budapest.jpg
    - assets/img/Hallstatt.jpg
    - assets/img/Hallstätter_See.jpg
    - assets/img/TowerBridge.jpg
    - assets/img/Birmingham.jpg
    - assets/img/BirminghamBoats.jpg
    - assets/img/BirminghamBCN.jpg
    - assets/img/empty.png
    - assets/img/addchart-24px.svg
    - assets/puzzles.json

  fonts:
    - family: Raleway
      fonts:
        - asset: assets/fonts/Raleway-Regular.ttf
